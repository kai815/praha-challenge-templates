export const thirtyOneLength: number[] = [...Array(31)].map((): number => 1);

export const thirtyLength: number[] = [...Array(30)].map((): number => 1);

export const twentyNineLength: number[] = [...Array(29)].map((): number => 1);
